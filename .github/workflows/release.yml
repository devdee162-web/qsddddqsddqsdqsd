name: Release TOOL OAP (EXE local via gh)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  skip-ci-build:
    runs-on: ubuntu-latest
    steps:
      - name: Release deja publiee en local
        run: echo "L'EXE est uploade par build_exe.bat via gh release."
